project: connect-button-storybook

image:
  tag: {{ .StateValues.ci.tag }}

ingress:
  enabled: true
  hosts:
    - host: connect-button-storybook-dev.rdx-works-main.extratools.works
      paths:
        - path: /
          pathType: Prefix
